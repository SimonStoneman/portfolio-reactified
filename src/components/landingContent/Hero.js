import '../../assets/styles/hero.css';

function Hero(props) {
    return (
        <section className="hero-banner">
        
            <h2>{props.name}</h2>
   
        </section>
    );
};

export default Hero;